import GroundScence from './GroundScene';
import './style.css'

window.addEventListener('DOMContentLoaded', ()=>{
    const canvas = document.getElementById('app');
    
    const game = new GroundScence({canvas})

})